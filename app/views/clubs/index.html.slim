#clubs

= render "templates/clubs/index"
= render "templates/clubs/club"

javascript:
  (function(){
    window.router = new StartupSoccerManager.Routers.ClubsRouter( { clubs: #{@clubs.to_json.html_safe} } );
    Backbone.history.start();
  })();