<%= form_with(model: club, local: true) do |form| %>
  <% if club.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(club.errors.count, "error") %> prohibited this club from being saved:</h2>

      <ul>
      <% club.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :founded_at %>
    <%= form.date_select :founded_at %>
  </div>

  <div class="field">
    <%= form.label :club_image_url %>
    <%= form.text_field :club_image_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
